return {
    refresh_key = "F7",
    toggle_key = "F8",
    world_map_markers = true,

    -- Display settings are independent from debug logging.
    -- Show player and treasure Z coordinates to help locate chests faster.
    show_height = true,
    show_treasure_types = true,

    -- Debug logging writes detailed save-state and nearby-treasure diagnostics.
    debug_logging = false,

}
